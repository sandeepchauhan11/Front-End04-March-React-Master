
// export function add(a, b) {
//     return a + b;
// }

// export const a = 100;

function add(a, b) {
    return a + b;
}

export function subtract(a, b) {
    return a - b;
}

export default add;

// export default subtract;

// named exports => we can make any number of named exports
// default export => We can atmost make only one default export from a module